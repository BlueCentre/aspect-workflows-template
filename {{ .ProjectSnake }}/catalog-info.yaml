# Backstage catalog-info.yaml
# See https://backstage.io/docs/features/software-catalog/descriptor-format
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  # Note: {{ "{{" }} .ProjectKebab {{ "}}" }} will be replaced by scaffold with the project name
  name: {{ "{{" }} .ProjectKebab {{ "}}" }}
  description: Bazel project using Aspect Workflows
  tags:
    - bazel
    - aspect-workflows
{{- if .Computed.javascript }}
    - javascript
    - typescript
{{- end }}
{{- if .Computed.python }}
    - python
{{- end }}
{{- if .Computed.go }}
    - go
{{- end }}
{{- if .Computed.java }}
    - java
{{- end }}
{{- if .Computed.kotlin }}
    - kotlin
{{- end }}
{{- if .Computed.cpp }}
    - cpp
{{- end }}
{{- if .Computed.rust }}
    - rust
{{- end }}
{{- if .Computed.shell }}
    - shell
{{- end }}
spec:
  type: service
  lifecycle: production
  # TODO: Update owner to match your team/organization
  owner: team-platform
